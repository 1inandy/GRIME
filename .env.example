# Copy this file to .env and fill in your values. .env is gitignored.

# Mapbox token used by api/main.py at /api/config.
# Use a publishable (pk.) token. Restrict it by URL in the Mapbox dashboard for production.
MAPBOX_TOKEN=
