# Secrets / config (never commit real tokens)
dashboard/config.js
.env

# Local data artifacts
data/
*.gpkg

# Pipeline runtime cache + byproducts (osmnx/py3dep HTTP cache, raw stream network)
cache/
mock_data/streams.geojson

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.ipynb_checkpoints/

# OS / editor cruft
.DS_Store

# Stray uvicorn redirect file (see L2)
/uvicorn
